<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/14 13:10:59 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.systemlog.dao.IUserLogDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.systemlog.entity.UserLog" id="resultMap">
        <id property="userLogCode" column="userlogcode" />
        <result property="userCode" column="usercode" />
        <result property="userName" column="username" />
        <result property="userLoginIp" column="userloginip" />
        <result property="operationType" column="operationtype" />
        <result property="description" column="description" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        userlogcode, usercode, username, userloginip, operationtype, description, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="userLogCode != null"><![CDATA[ and userlogcode like #{userLogCode} ]]></if>
            <if test="userLogCode_enum != null">
                <![CDATA[ and userlogcode in ]]>
                <foreach collection="userLogCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="userCode != null"><![CDATA[ and usercode like #{userCode} ]]></if>
            <if test="userCode_enum != null">
                <![CDATA[ and usercode in ]]>
                <foreach collection="userCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="userName != null"><![CDATA[ and username like #{userName} ]]></if>
            <if test="userName_enum != null">
                <![CDATA[ and username in ]]>
                <foreach collection="userName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="userLoginIp != null"><![CDATA[ and userloginip like #{userLoginIp} ]]></if>
            <if test="userLoginIp_enum != null">
                <![CDATA[ and userloginip in ]]>
                <foreach collection="userLoginIp_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="operationType != null"><![CDATA[ and operationtype like #{operationType} ]]></if>
            <if test="operationType_enum != null">
                <![CDATA[ and operationtype in ]]>
                <foreach collection="operationType_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="description != null"><![CDATA[ and description like #{description} ]]></if>
            <if test="description_enum != null">
                <![CDATA[ and description in ]]>
                <foreach collection="description_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'userLogCode'"><![CDATA[ userlogcode like #{item.value} ]]></if>
                    <if test="item.name == 'userCode'"><![CDATA[ usercode like #{item.value} ]]></if>
                    <if test="item.name == 'userName'"><![CDATA[ username like #{item.value} ]]></if>
                    <if test="item.name == 'userLoginIp'"><![CDATA[ userloginip like #{item.value} ]]></if>
                    <if test="item.name == 'operationType'"><![CDATA[ operationtype like #{item.value} ]]></if>
                    <if test="item.name == 'description'"><![CDATA[ description like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'userLogCode_desc'"><![CDATA[ userlogcode desc, ]]></if>
            <if test="_orderBy == 'userLogCode_asc'"><![CDATA[ userlogcode, ]]></if>
            <if test="_orderBy == 'userCode_desc'"><![CDATA[ usercode desc, ]]></if>
            <if test="_orderBy == 'userCode_asc'"><![CDATA[ usercode, ]]></if>
            <if test="_orderBy == 'userName_desc'"><![CDATA[ username desc, ]]></if>
            <if test="_orderBy == 'userName_asc'"><![CDATA[ username, ]]></if>
            <if test="_orderBy == 'userLoginIp_desc'"><![CDATA[ userloginip desc, ]]></if>
            <if test="_orderBy == 'userLoginIp_asc'"><![CDATA[ userloginip, ]]></if>
            <if test="_orderBy == 'operationType_desc'"><![CDATA[ operationtype desc, ]]></if>
            <if test="_orderBy == 'operationType_asc'"><![CDATA[ operationtype, ]]></if>
            <if test="_orderBy == 'description_desc'"><![CDATA[ description desc, ]]></if>
            <if test="_orderBy == 'description_asc'"><![CDATA[ description, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.systemlog.entity.UserLog">
        insert into
        dyb_userlog(userlogcode,usercode,username,userloginip,operationtype,description,createtime)
        values(#{userLogCode},#{userCode},#{userName},#{userLoginIp},#{operationType},#{description},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.systemlog.entity.UserLog">
        update dyb_userlog
        set usercode=#{userCode},username=#{userName},userloginip=#{userLoginIp},operationtype=#{operationType},description=#{description},createtime=#{createTime}
        where userlogcode=#{userLogCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_userlog where userlogcode=#{userLogCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_userlog
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_userlog where userlogcode=#{userLogCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_userlog where userlogcode=#{userLogCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_userlog
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_userlog
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_userlog
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


