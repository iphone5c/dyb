<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/11 15:57:29 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.user.dao.IPermissionsDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.user.entity.Permissions" id="resultMap">
        <id property="permissionsCode" column="permissionscode" />
        <result property="sequence" column="sequence" />
        <result property="permissionsName" column="permissionsname" />
        <result property="description" column="description" />
        <result property="createTime" column="createtime" />
        <result property="leaf" column="leaf" />
        <result property="permissionsUrl" column="permissionsurl" />
        <result property="image" column="image" />
        <result property="parentCode" column="parentcode" />
    </resultMap>

    <sql id="baseCol">
        permissionscode, sequence, permissionsname, description, createtime, leaf, permissionsurl, image, parentcode
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="permissionsCode != null"><![CDATA[ and permissionscode like #{permissionsCode} ]]></if>
            <if test="permissionsCode_enum != null">
                <![CDATA[ and permissionscode in ]]>
                <foreach collection="permissionsCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="sequence != null"><![CDATA[ and sequence = #{sequence} ]]></if>
            <if test="sequence_r_min != null"><![CDATA[ and sequence >= #{sequence_r_min} and sequence <= #{sequence_r_max} ]]></if>
            <if test="sequence_enum != null">
                <![CDATA[ and sequence in ]]>
                <foreach collection="sequence_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="permissionsName != null"><![CDATA[ and permissionsname like #{permissionsName} ]]></if>
            <if test="permissionsName_enum != null">
                <![CDATA[ and permissionsname in ]]>
                <foreach collection="permissionsName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="description != null"><![CDATA[ and description like #{description} ]]></if>
            <if test="description_enum != null">
                <![CDATA[ and description in ]]>
                <foreach collection="description_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="leaf != null"><![CDATA[ and leaf = #{leaf} ]]></if>
            <if test="permissionsUrl != null"><![CDATA[ and permissionsurl like #{permissionsUrl} ]]></if>
            <if test="permissionsUrl_enum != null">
                <![CDATA[ and permissionsurl in ]]>
                <foreach collection="permissionsUrl_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="image != null"><![CDATA[ and image like #{image} ]]></if>
            <if test="image_enum != null">
                <![CDATA[ and image in ]]>
                <foreach collection="image_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="parentCode != null"><![CDATA[ and parentcode like #{parentCode} ]]></if>
            <if test="parentCode_enum != null">
                <![CDATA[ and parentcode in ]]>
                <foreach collection="parentCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'permissionsCode'"><![CDATA[ permissionscode like #{item.value} ]]></if>
                    <if test="item.name == 'permissionsName'"><![CDATA[ permissionsname like #{item.value} ]]></if>
                    <if test="item.name == 'description'"><![CDATA[ description like #{item.value} ]]></if>
                    <if test="item.name == 'permissionsUrl'"><![CDATA[ permissionsurl like #{item.value} ]]></if>
                    <if test="item.name == 'image'"><![CDATA[ image like #{item.value} ]]></if>
                    <if test="item.name == 'parentCode'"><![CDATA[ parentcode like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'permissionsCode_desc'"><![CDATA[ permissionscode desc, ]]></if>
            <if test="_orderBy == 'permissionsCode_asc'"><![CDATA[ permissionscode, ]]></if>
            <if test="_orderBy == 'sequence_desc'"><![CDATA[ sequence desc, ]]></if>
            <if test="_orderBy == 'sequence_asc'"><![CDATA[ sequence, ]]></if>
            <if test="_orderBy == 'permissionsName_desc'"><![CDATA[ permissionsname desc, ]]></if>
            <if test="_orderBy == 'permissionsName_asc'"><![CDATA[ permissionsname, ]]></if>
            <if test="_orderBy == 'description_desc'"><![CDATA[ description desc, ]]></if>
            <if test="_orderBy == 'description_asc'"><![CDATA[ description, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
            <if test="_orderBy == 'leaf_desc'"><![CDATA[ leaf desc, ]]></if>
            <if test="_orderBy == 'leaf_asc'"><![CDATA[ leaf, ]]></if>
            <if test="_orderBy == 'permissionsUrl_desc'"><![CDATA[ permissionsurl desc, ]]></if>
            <if test="_orderBy == 'permissionsUrl_asc'"><![CDATA[ permissionsurl, ]]></if>
            <if test="_orderBy == 'image_desc'"><![CDATA[ image desc, ]]></if>
            <if test="_orderBy == 'image_asc'"><![CDATA[ image, ]]></if>
            <if test="_orderBy == 'parentCode_desc'"><![CDATA[ parentcode desc, ]]></if>
            <if test="_orderBy == 'parentCode_asc'"><![CDATA[ parentcode, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.user.entity.Permissions">
        insert into
        dyb_permissions(permissionscode,sequence,permissionsname,description,createtime,leaf,permissionsurl,image,parentcode)
        values(#{permissionsCode},#{sequence},#{permissionsName},#{description},#{createTime},#{leaf},#{permissionsUrl},#{image},#{parentCode});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.user.entity.Permissions">
        update dyb_permissions
        set sequence=#{sequence},permissionsname=#{permissionsName},description=#{description},createtime=#{createTime},leaf=#{leaf},permissionsurl=#{permissionsUrl},image=#{image},parentcode=#{parentCode}
        where permissionscode=#{permissionsCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_permissions where permissionscode=#{permissionsCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_permissions
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_permissions where permissionscode=#{permissionsCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_permissions where permissionscode=#{permissionsCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_permissions
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_permissions
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_permissions
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


