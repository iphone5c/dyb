<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/18 19:24:05 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.businessprocess.dao.IBusinessPaperDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessPaper" id="resultMap">
        <id property="businessPaperCode" column="businesspapercode" />
        <result property="paperName" column="papername" />
        <result property="paperType" column="papertype" />
        <result property="docCode" column="doccode" />
    </resultMap>

    <sql id="baseCol">
        businesspapercode, papername, papertype, doccode
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="businessPaperCode != null"><![CDATA[ and businesspapercode like #{businessPaperCode} ]]></if>
            <if test="businessPaperCode_enum != null">
                <![CDATA[ and businesspapercode in ]]>
                <foreach collection="businessPaperCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="paperName != null"><![CDATA[ and papername like #{paperName} ]]></if>
            <if test="paperName_enum != null">
                <![CDATA[ and papername in ]]>
                <foreach collection="paperName_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="paperType != null"><![CDATA[ and papertype like #{paperType} ]]></if>
            <if test="paperType_enum != null">
                <![CDATA[ and papertype in ]]>
                <foreach collection="paperType_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="docCode != null"><![CDATA[ and doccode like #{docCode} ]]></if>
            <if test="docCode_enum != null">
                <![CDATA[ and doccode in ]]>
                <foreach collection="docCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'businessPaperCode'"><![CDATA[ businesspapercode like #{item.value} ]]></if>
                    <if test="item.name == 'paperName'"><![CDATA[ papername like #{item.value} ]]></if>
                    <if test="item.name == 'paperType'"><![CDATA[ papertype like #{item.value} ]]></if>
                    <if test="item.name == 'docCode'"><![CDATA[ doccode like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'businessPaperCode_desc'"><![CDATA[ businesspapercode desc, ]]></if>
            <if test="_orderBy == 'businessPaperCode_asc'"><![CDATA[ businesspapercode, ]]></if>
            <if test="_orderBy == 'paperName_desc'"><![CDATA[ papername desc, ]]></if>
            <if test="_orderBy == 'paperName_asc'"><![CDATA[ papername, ]]></if>
            <if test="_orderBy == 'paperType_desc'"><![CDATA[ papertype desc, ]]></if>
            <if test="_orderBy == 'paperType_asc'"><![CDATA[ papertype, ]]></if>
            <if test="_orderBy == 'docCode_desc'"><![CDATA[ doccode desc, ]]></if>
            <if test="_orderBy == 'docCode_asc'"><![CDATA[ doccode, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessPaper">
        insert into
        dyb_businesspaper(businesspapercode,papername,papertype,doccode)
        values(#{businessPaperCode},#{paperName},#{paperType},#{docCode});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessPaper">
        update dyb_businesspaper
        set papername=#{paperName},papertype=#{paperType},doccode=#{docCode}
        where businesspapercode=#{businessPaperCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_businesspaper where businesspapercode=#{businessPaperCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_businesspaper
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_businesspaper where businesspapercode=#{businessPaperCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_businesspaper where businesspapercode=#{businessPaperCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_businesspaper
        <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_businesspaper
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_businesspaper
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


