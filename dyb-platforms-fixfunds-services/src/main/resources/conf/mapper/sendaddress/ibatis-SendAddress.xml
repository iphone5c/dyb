<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/27 20:18:11 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.sendaddress.dao.ISendAddressDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.sendaddress.entity.SendAddress" id="resultMap">
        <id property="sendAddressCode" column="sendaddresscode" />
        <result property="receiver" column="receiver" />
        <result property="province" column="province" />
        <result property="city" column="city" />
        <result property="address" column="address" />
        <result property="postalcode" column="postalcode" />
        <result property="phone" column="phone" />
        <result property="defaultChecked" column="defaultchecked" />
        <result property="createTime" column="createtime" />
        <result property="accountCode" column="accountcode" />
    </resultMap>

    <sql id="baseCol">
        sendaddresscode, receiver, province, city, address, postalcode, phone, defaultchecked, createtime, accountcode
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="sendAddressCode != null"><![CDATA[ and sendaddresscode like #{sendAddressCode} ]]></if>
            <if test="sendAddressCode_enum != null">
                <![CDATA[ and sendaddresscode in ]]>
                <foreach collection="sendAddressCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="receiver != null"><![CDATA[ and receiver like #{receiver} ]]></if>
            <if test="receiver_enum != null">
                <![CDATA[ and receiver in ]]>
                <foreach collection="receiver_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="province != null"><![CDATA[ and province like #{province} ]]></if>
            <if test="province_enum != null">
                <![CDATA[ and province in ]]>
                <foreach collection="province_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="city != null"><![CDATA[ and city like #{city} ]]></if>
            <if test="city_enum != null">
                <![CDATA[ and city in ]]>
                <foreach collection="city_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="address != null"><![CDATA[ and address like #{address} ]]></if>
            <if test="address_enum != null">
                <![CDATA[ and address in ]]>
                <foreach collection="address_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="postalcode != null"><![CDATA[ and postalcode like #{postalcode} ]]></if>
            <if test="postalcode_enum != null">
                <![CDATA[ and postalcode in ]]>
                <foreach collection="postalcode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="phone != null"><![CDATA[ and phone like #{phone} ]]></if>
            <if test="phone_enum != null">
                <![CDATA[ and phone in ]]>
                <foreach collection="phone_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="defaultChecked != null"><![CDATA[ and defaultchecked = #{defaultChecked} ]]></if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="accountCode != null"><![CDATA[ and accountcode like #{accountCode} ]]></if>
            <if test="accountCode_enum != null">
                <![CDATA[ and accountcode in ]]>
                <foreach collection="accountCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'sendAddressCode'"><![CDATA[ sendaddresscode like #{item.value} ]]></if>
                    <if test="item.name == 'receiver'"><![CDATA[ receiver like #{item.value} ]]></if>
                    <if test="item.name == 'province'"><![CDATA[ province like #{item.value} ]]></if>
                    <if test="item.name == 'city'"><![CDATA[ city like #{item.value} ]]></if>
                    <if test="item.name == 'address'"><![CDATA[ address like #{item.value} ]]></if>
                    <if test="item.name == 'postalcode'"><![CDATA[ postalcode like #{item.value} ]]></if>
                    <if test="item.name == 'phone'"><![CDATA[ phone like #{item.value} ]]></if>
                    <if test="item.name == 'accountCode'"><![CDATA[ accountcode like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'sendAddressCode_desc'"><![CDATA[ sendaddresscode desc, ]]></if>
            <if test="_orderBy == 'sendAddressCode_asc'"><![CDATA[ sendaddresscode, ]]></if>
            <if test="_orderBy == 'receiver_desc'"><![CDATA[ receiver desc, ]]></if>
            <if test="_orderBy == 'receiver_asc'"><![CDATA[ receiver, ]]></if>
            <if test="_orderBy == 'province_desc'"><![CDATA[ province desc, ]]></if>
            <if test="_orderBy == 'province_asc'"><![CDATA[ province, ]]></if>
            <if test="_orderBy == 'city_desc'"><![CDATA[ city desc, ]]></if>
            <if test="_orderBy == 'city_asc'"><![CDATA[ city, ]]></if>
            <if test="_orderBy == 'address_desc'"><![CDATA[ address desc, ]]></if>
            <if test="_orderBy == 'address_asc'"><![CDATA[ address, ]]></if>
            <if test="_orderBy == 'postalcode_desc'"><![CDATA[ postalcode desc, ]]></if>
            <if test="_orderBy == 'postalcode_asc'"><![CDATA[ postalcode, ]]></if>
            <if test="_orderBy == 'phone_desc'"><![CDATA[ phone desc, ]]></if>
            <if test="_orderBy == 'phone_asc'"><![CDATA[ phone, ]]></if>
            <if test="_orderBy == 'defaultChecked_desc'"><![CDATA[ defaultchecked desc, ]]></if>
            <if test="_orderBy == 'defaultChecked_asc'"><![CDATA[ defaultchecked, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
            <if test="_orderBy == 'accountCode_desc'"><![CDATA[ accountcode desc, ]]></if>
            <if test="_orderBy == 'accountCode_asc'"><![CDATA[ accountcode, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.sendaddress.entity.SendAddress">
        insert into
        dyb_sendaddress(sendaddresscode,receiver,province,city,address,postalcode,phone,defaultchecked,createtime,accountcode)
        values(#{sendAddressCode},#{receiver},#{province},#{city},#{address},#{postalcode},#{phone},#{defaultChecked},#{createTime},#{accountCode});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.sendaddress.entity.SendAddress">
        update dyb_sendaddress
        set receiver=#{receiver},province=#{province},city=#{city},address=#{address},postalcode=#{postalcode},phone=#{phone},defaultchecked=#{defaultChecked},createtime=#{createTime},accountcode=#{accountCode}
        where sendaddresscode=#{sendAddressCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_sendaddress where sendaddresscode=#{sendAddressCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_sendaddress
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_sendaddress where sendaddresscode=#{sendAddressCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_sendaddress where sendaddresscode=#{sendAddressCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_sendaddress
        <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_sendaddress
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_sendaddress
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


