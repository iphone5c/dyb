<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/10/8 15:36:19 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.blacklist.dao.IBlacklistDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.blacklist.entity.Blacklist" id="resultMap">
        <id property="blacklistCode" column="blacklistcode" />
        <result property="merchantCode" column="merchantcode" />
        <result property="blackDescption" column="blackdescption" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        blacklistcode, merchantcode, blackdescption, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="blacklistCode != null"><![CDATA[ and blacklistcode like #{blacklistCode} ]]></if>
            <if test="blacklistCode_enum != null">
                <![CDATA[ and blacklistcode in ]]>
                <foreach collection="blacklistCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="merchantCode != null"><![CDATA[ and merchantcode like #{merchantCode} ]]></if>
            <if test="merchantCode_enum != null">
                <![CDATA[ and merchantcode in ]]>
                <foreach collection="merchantCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="blackDescption != null"><![CDATA[ and blackdescption like #{blackDescption} ]]></if>
            <if test="blackDescption_enum != null">
                <![CDATA[ and blackdescption in ]]>
                <foreach collection="blackDescption_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'blacklistCode'"><![CDATA[ blacklistcode like #{item.value} ]]></if>
                    <if test="item.name == 'merchantCode'"><![CDATA[ merchantcode like #{item.value} ]]></if>
                    <if test="item.name == 'blackDescption'"><![CDATA[ blackdescption like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'blacklistCode_desc'"><![CDATA[ blacklistcode desc, ]]></if>
            <if test="_orderBy == 'blacklistCode_asc'"><![CDATA[ blacklistcode, ]]></if>
            <if test="_orderBy == 'merchantCode_desc'"><![CDATA[ merchantcode desc, ]]></if>
            <if test="_orderBy == 'merchantCode_asc'"><![CDATA[ merchantcode, ]]></if>
            <if test="_orderBy == 'blackDescption_desc'"><![CDATA[ blackdescption desc, ]]></if>
            <if test="_orderBy == 'blackDescption_asc'"><![CDATA[ blackdescption, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.blacklist.entity.Blacklist">
        insert into
        dyb_blacklist(blacklistcode,merchantcode,blackdescption,createtime)
        values(#{blacklistCode},#{merchantCode},#{blackDescption},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.blacklist.entity.Blacklist">
        update dyb_blacklist
        set merchantcode=#{merchantCode},blackdescption=#{blackDescption},createtime=#{createTime}
        where blacklistcode=#{blacklistCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_blacklist where blacklistcode=#{blacklistCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_blacklist
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_blacklist where blacklistcode=#{blacklistCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_blacklist where blacklistcode=#{blacklistCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_blacklist
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_blacklist
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_blacklist
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


