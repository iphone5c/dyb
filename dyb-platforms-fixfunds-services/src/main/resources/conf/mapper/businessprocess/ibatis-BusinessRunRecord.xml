<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/18 19:24:06 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.businessprocess.dao.IBusinessRunRecordDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessRunRecord" id="resultMap">
        <id property="businessRunRecordCode" column="businessrunrecordcode" />
        <result property="runner" column="runner" />
        <result property="runTime" column="runtime" />
        <result property="content" column="content" />
        <result property="docCode" column="doccode" />
        <result property="businessStatus" column="businessstatus" />
    </resultMap>

    <sql id="baseCol">
        businessrunrecordcode, runner, runtime, content, doccode, businessstatus
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="businessRunRecordCode != null"><![CDATA[ and businessrunrecordcode like #{businessRunRecordCode} ]]></if>
            <if test="businessRunRecordCode_enum != null">
                <![CDATA[ and businessrunrecordcode in ]]>
                <foreach collection="businessRunRecordCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="runner != null"><![CDATA[ and runner like #{runner} ]]></if>
            <if test="runner_enum != null">
                <![CDATA[ and runner in ]]>
                <foreach collection="runner_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="runTime != null"><![CDATA[ and runtime = #{runTime} ]]></if>
            <if test="runTime_r_min != null"><![CDATA[ and runtime >= #{runTime_r_min} and runtime < #{runTime_r_max} ]]></if>
            <if test="content != null"><![CDATA[ and content like #{content} ]]></if>
            <if test="content_enum != null">
                <![CDATA[ and content in ]]>
                <foreach collection="content_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="docCode != null"><![CDATA[ and doccode like #{docCode} ]]></if>
            <if test="docCode_enum != null">
                <![CDATA[ and doccode in ]]>
                <foreach collection="docCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="businessStatus != null"><![CDATA[ and businessstatus like #{businessStatus} ]]></if>
            <if test="businessStatus_enum != null">
                <![CDATA[ and businessstatus in ]]>
                <foreach collection="businessStatus_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'businessRunRecordCode'"><![CDATA[ businessrunrecordcode like #{item.value} ]]></if>
                    <if test="item.name == 'runner'"><![CDATA[ runner like #{item.value} ]]></if>
                    <if test="item.name == 'content'"><![CDATA[ content like #{item.value} ]]></if>
                    <if test="item.name == 'docCode'"><![CDATA[ doccode like #{item.value} ]]></if>
                    <if test="item.name == 'businessStatus'"><![CDATA[ businessstatus like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'businessRunRecordCode_desc'"><![CDATA[ businessrunrecordcode desc, ]]></if>
            <if test="_orderBy == 'businessRunRecordCode_asc'"><![CDATA[ businessrunrecordcode, ]]></if>
            <if test="_orderBy == 'runner_desc'"><![CDATA[ runner desc, ]]></if>
            <if test="_orderBy == 'runner_asc'"><![CDATA[ runner, ]]></if>
            <if test="_orderBy == 'runTime_desc'"><![CDATA[ runtime desc, ]]></if>
            <if test="_orderBy == 'runTime_asc'"><![CDATA[ runtime, ]]></if>
            <if test="_orderBy == 'content_desc'"><![CDATA[ content desc, ]]></if>
            <if test="_orderBy == 'content_asc'"><![CDATA[ content, ]]></if>
            <if test="_orderBy == 'docCode_desc'"><![CDATA[ doccode desc, ]]></if>
            <if test="_orderBy == 'docCode_asc'"><![CDATA[ doccode, ]]></if>
            <if test="_orderBy == 'businessStatus_desc'"><![CDATA[ businessstatus desc, ]]></if>
            <if test="_orderBy == 'businessStatus_asc'"><![CDATA[ businessstatus, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessRunRecord">
        insert into
        dyb_businessrunrecord(businessrunrecordcode,runner,runtime,content,doccode,businessstatus)
        values(#{businessRunRecordCode},#{runner},#{runTime},#{content},#{docCode},#{businessStatus});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.businessprocess.entity.BusinessRunRecord">
        update dyb_businessrunrecord
        set runner=#{runner},runtime=#{runTime},content=#{content},doccode=#{docCode},businessstatus=#{businessStatus}
        where businessrunrecordcode=#{businessRunRecordCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_businessrunrecord where businessrunrecordcode=#{businessRunRecordCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_businessrunrecord
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_businessrunrecord where businessrunrecordcode=#{businessRunRecordCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_businessrunrecord where businessrunrecordcode=#{businessRunRecordCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_businessrunrecord
        <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select
        <include refid="baseCol" />
        from dyb_businessrunrecord
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_businessrunrecord
        <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 