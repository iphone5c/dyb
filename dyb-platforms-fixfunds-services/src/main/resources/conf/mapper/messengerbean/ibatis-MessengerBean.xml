<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/10/10 10:42:00 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.messengerbean.dao.IMessengerBeanDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.messengerbean.entity.MessengerBean" id="resultMap">
        <id property="messengerBeanCode" column="messengerbeancode" />
        <result property="messengerBean" column="messengerbean" />
        <result property="messengerBeanType" column="messengerbeantype" />
        <result property="accountCode" column="accountcode" />
        <result property="freeze" column="freeze" />
    </resultMap>

    <sql id="baseCol">
        messengerbeancode, messengerbean, messengerbeantype, accountcode
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="messengerBeanCode != null"><![CDATA[ and messengerbeancode like #{messengerBeanCode} ]]></if>
            <if test="messengerBeanCode_enum != null">
                <![CDATA[ and messengerbeancode in ]]>
                <foreach collection="messengerBeanCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="messengerBean != null"><![CDATA[ and messengerbean = #{messengerBean} ]]></if>
            <if test="messengerBean_r_min != null"><![CDATA[ and messengerbean >= #{messengerBean_r_min} and messengerbean <= #{messengerBean_r_max} ]]></if>
            <if test="messengerBean_enum != null">
                <![CDATA[ and messengerbean in ]]>
                <foreach collection="messengerBean_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="freeze != null"><![CDATA[ and freeze = #{freeze} ]]></if>
            <if test="freeze_r_min != null"><![CDATA[ and freeze >= #{freeze_r_min} and freeze <= #{freeze_r_max} ]]></if>
            <if test="freeze_enum != null">
                <![CDATA[ and freeze in ]]>
                <foreach collection="freeze_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="messengerBeanType != null"><![CDATA[ and messengerbeantype like #{messengerBeanType} ]]></if>
            <if test="messengerBeanType_enum != null">
                <![CDATA[ and messengerbeantype in ]]>
                <foreach collection="messengerBeanType_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="accountCode != null"><![CDATA[ and accountcode like #{accountCode} ]]></if>
            <if test="accountCode_enum != null">
                <![CDATA[ and accountcode in ]]>
                <foreach collection="accountCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'messengerBeanCode'"><![CDATA[ messengerbeancode like #{item.value} ]]></if>
                    <if test="item.name == 'messengerBeanType'"><![CDATA[ messengerbeantype like #{item.value} ]]></if>
                    <if test="item.name == 'accountCode'"><![CDATA[ accountcode like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'messengerBeanCode_desc'"><![CDATA[ messengerbeancode desc, ]]></if>
            <if test="_orderBy == 'messengerBeanCode_asc'"><![CDATA[ messengerbeancode, ]]></if>
            <if test="_orderBy == 'messengerBean_desc'"><![CDATA[ messengerbean desc, ]]></if>
            <if test="_orderBy == 'messengerBean_asc'"><![CDATA[ messengerbean, ]]></if>
            <if test="_orderBy == 'messengerBeanType_desc'"><![CDATA[ messengerbeantype desc, ]]></if>
            <if test="_orderBy == 'messengerBeanType_asc'"><![CDATA[ messengerbeantype, ]]></if>
            <if test="_orderBy == 'accountCode_desc'"><![CDATA[ accountcode desc, ]]></if>
            <if test="_orderBy == 'accountCode_asc'"><![CDATA[ accountcode, ]]></if>
            <if test="_orderBy == 'freeze_desc'"><![CDATA[ freeze desc, ]]></if>
            <if test="_orderBy == 'freeze_asc'"><![CDATA[ freeze, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.messengerbean.entity.MessengerBean">
        insert into
        dyb_messengerbean(messengerbeancode,messengerbean,messengerbeantype,accountcode,freeze)
        values(#{messengerBeanCode},#{messengerBean},#{messengerBeanType},#{accountCode},#{freeze});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.messengerbean.entity.MessengerBean">
        update dyb_messengerbean
        set messengerbean=#{messengerBean},messengerbeantype=#{messengerBeanType},accountcode=#{accountCode},freeze=#{freeze}
        where messengerbeancode=#{messengerBeanCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_messengerbean where messengerbeancode=#{messengerBeanCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_messengerbean
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_messengerbean where messengerbeancode=#{messengerBeanCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_messengerbean where messengerbeancode=#{messengerBeanCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_messengerbean
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_messengerbean
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_messengerbean
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


