<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/10/10 10:40:33 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.conversion.dao.IConversionDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.conversion.entity.Conversion" id="resultMap">
        <id property="conversionCode" column="conversioncode" />
        <result property="conversionType" column="conversiontype" />
        <result property="applyConversionNum" column="applyconversionnum" />
        <result property="conversionNum" column="conversionnum" />
        <result property="deductions" column="deductions" />
        <result property="conversionAccount" column="conversionaccount" />
        <result property="applyTime" column="applytime" />
        <result property="applyStatus" column="applystatus" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        conversioncode, conversiontype, applyconversionnum, conversionnum, deductions, conversionaccount, applytime, applystatus, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="conversionCode != null"><![CDATA[ and conversioncode like #{conversionCode} ]]></if>
            <if test="conversionCode_enum != null">
                <![CDATA[ and conversioncode in ]]>
                <foreach collection="conversionCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="conversionType != null"><![CDATA[ and conversiontype like #{conversionType} ]]></if>
            <if test="conversionType_enum != null">
                <![CDATA[ and conversiontype in ]]>
                <foreach collection="conversionType_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="applyConversionNum != null"><![CDATA[ and applyconversionnum = #{applyConversionNum} ]]></if>
            <if test="applyConversionNum_r_min != null"><![CDATA[ and applyconversionnum >= #{applyConversionNum_r_min} and applyconversionnum <= #{applyConversionNum_r_max} ]]></if>
            <if test="applyConversionNum_enum != null">
                <![CDATA[ and applyconversionnum in ]]>
                <foreach collection="applyConversionNum_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="conversionNum != null"><![CDATA[ and conversionnum = #{conversionNum} ]]></if>
            <if test="conversionNum_r_min != null"><![CDATA[ and conversionnum >= #{conversionNum_r_min} and conversionnum <= #{conversionNum_r_max} ]]></if>
            <if test="conversionNum_enum != null">
                <![CDATA[ and conversionnum in ]]>
                <foreach collection="conversionNum_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="deductions != null"><![CDATA[ and deductions = #{deductions} ]]></if>
            <if test="deductions_r_min != null"><![CDATA[ and deductions >= #{deductions_r_min} and deductions <= #{deductions_r_max} ]]></if>
            <if test="deductions_enum != null">
                <![CDATA[ and deductions in ]]>
                <foreach collection="deductions_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="conversionAccount != null"><![CDATA[ and conversionaccount like #{conversionAccount} ]]></if>
            <if test="conversionAccount_enum != null">
                <![CDATA[ and conversionaccount in ]]>
                <foreach collection="conversionAccount_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="applyTime != null"><![CDATA[ and applytime = #{applyTime} ]]></if>
            <if test="applyTime_r_min != null"><![CDATA[ and applytime >= #{applyTime_r_min} and applytime < #{applyTime_r_max} ]]></if>
            <if test="applyStatus != null"><![CDATA[ and applystatus like #{applyStatus} ]]></if>
            <if test="applyStatus_enum != null">
                <![CDATA[ and applystatus in ]]>
                <foreach collection="applyStatus_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'conversionCode'"><![CDATA[ conversioncode like #{item.value} ]]></if>
                    <if test="item.name == 'conversionType'"><![CDATA[ conversiontype like #{item.value} ]]></if>
                    <if test="item.name == 'conversionAccount'"><![CDATA[ conversionaccount like #{item.value} ]]></if>
                    <if test="item.name == 'applyStatus'"><![CDATA[ applystatus like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'conversionCode_desc'"><![CDATA[ conversioncode desc, ]]></if>
            <if test="_orderBy == 'conversionCode_asc'"><![CDATA[ conversioncode, ]]></if>
            <if test="_orderBy == 'conversionType_desc'"><![CDATA[ conversiontype desc, ]]></if>
            <if test="_orderBy == 'conversionType_asc'"><![CDATA[ conversiontype, ]]></if>
            <if test="_orderBy == 'applyConversionNum_desc'"><![CDATA[ applyconversionnum desc, ]]></if>
            <if test="_orderBy == 'applyConversionNum_asc'"><![CDATA[ applyconversionnum, ]]></if>
            <if test="_orderBy == 'conversionNum_desc'"><![CDATA[ conversionnum desc, ]]></if>
            <if test="_orderBy == 'conversionNum_asc'"><![CDATA[ conversionnum, ]]></if>
            <if test="_orderBy == 'deductions_desc'"><![CDATA[ deductions desc, ]]></if>
            <if test="_orderBy == 'deductions_asc'"><![CDATA[ deductions, ]]></if>
            <if test="_orderBy == 'conversionAccount_desc'"><![CDATA[ conversionaccount desc, ]]></if>
            <if test="_orderBy == 'conversionAccount_asc'"><![CDATA[ conversionaccount, ]]></if>
            <if test="_orderBy == 'applyTime_desc'"><![CDATA[ applytime desc, ]]></if>
            <if test="_orderBy == 'applyTime_asc'"><![CDATA[ applytime, ]]></if>
            <if test="_orderBy == 'applyStatus_desc'"><![CDATA[ applystatus desc, ]]></if>
            <if test="_orderBy == 'applyStatus_asc'"><![CDATA[ applystatus, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.conversion.entity.Conversion">
        insert into
        dyb_conversion(conversioncode,conversiontype,applyconversionnum,conversionnum,deductions,conversionaccount,applytime,applystatus,createtime)
        values(#{conversionCode},#{conversionType},#{applyConversionNum},#{conversionNum},#{deductions},#{conversionAccount},#{applyTime},#{applyStatus},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.conversion.entity.Conversion">
        update dyb_conversion
        set conversiontype=#{conversionType},applyconversionnum=#{applyConversionNum},conversionnum=#{conversionNum},deductions=#{deductions},conversionaccount=#{conversionAccount},applytime=#{applyTime},applystatus=#{applyStatus},createtime=#{createTime}
        where conversioncode=#{conversionCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_conversion where conversioncode=#{conversionCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_conversion
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_conversion where conversioncode=#{conversionCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_conversion where conversioncode=#{conversionCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_conversion
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_conversion
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_conversion
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


