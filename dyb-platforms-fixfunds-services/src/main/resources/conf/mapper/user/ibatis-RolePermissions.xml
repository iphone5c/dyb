<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/9/11 15:51:42 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.user.dao.IRolePermissionsDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.user.entity.RolePermissions" id="resultMap">
        <id property="roleCode" column="rolecode" />
        <result property="permissionsCode" column="permissionscode" />
    </resultMap>

    <sql id="baseCol">
        rolecode, permissionscode
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="roleCode != null"><![CDATA[ and rolecode like #{roleCode} ]]></if>
            <if test="roleCode_enum != null">
                <![CDATA[ and rolecode in ]]>
                <foreach collection="roleCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="permissionsCode != null"><![CDATA[ and permissionscode like #{permissionsCode} ]]></if>
            <if test="permissionsCode_enum != null">
                <![CDATA[ and permissionscode in ]]>
                <foreach collection="permissionsCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'roleCode'"><![CDATA[ rolecode like #{item.value} ]]></if>
                    <if test="item.name == 'permissionsCode'"><![CDATA[ permissionscode like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'roleCode_desc'"><![CDATA[ rolecode desc, ]]></if>
            <if test="_orderBy == 'roleCode_asc'"><![CDATA[ rolecode, ]]></if>
            <if test="_orderBy == 'permissionsCode_desc'"><![CDATA[ permissionscode desc, ]]></if>
            <if test="_orderBy == 'permissionsCode_asc'"><![CDATA[ permissionscode, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.user.entity.RolePermissions">
        insert into
        dyb_rolepermissions(rolecode,permissionscode)
        values(#{roleCode},#{permissionsCode});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.user.entity.RolePermissions">
        update dyb_rolepermissions
        set permissionscode=#{permissionsCode}
        where rolecode=#{roleCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_rolepermissions where rolecode=#{roleCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_rolepermissions
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_rolepermissions where rolecode=#{roleCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_rolepermissions where rolecode=#{roleCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_rolepermissions
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_rolepermissions
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_rolepermissions
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


