<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 2016/10/10 10:31:05 lenovo created. -->
<!-- Generated by Caven.CodeBuilder (funiJava.mybatis_map 1.0). -->

<mapper namespace="com.dyb.platforms.fixfunds.services.business.transfer.dao.ITransferDao">

    <resultMap type="com.dyb.platforms.fixfunds.services.business.transfer.entity.Transfer" id="resultMap">
        <id property="transferCode" column="transfercode" />
        <result property="transferAccount" column="transferaccount" />
        <result property="gainAccount" column="gainaccount" />
        <result property="transferTime" column="transfertime" />
        <result property="messengerBean" column="messengerbean" />
        <result property="createTime" column="createtime" />
    </resultMap>

    <sql id="baseCol">
        transfercode, transferaccount, gainaccount, transfertime, messengerbean, createtime
    </sql>

    <sql id="whereSql">
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="transferCode != null"><![CDATA[ and transfercode like #{transferCode} ]]></if>
            <if test="transferCode_enum != null">
                <![CDATA[ and transfercode in ]]>
                <foreach collection="transferCode_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="transferAccount != null"><![CDATA[ and transferaccount like #{transferAccount} ]]></if>
            <if test="transferAccount_enum != null">
                <![CDATA[ and transferaccount in ]]>
                <foreach collection="transferAccount_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="gainAccount != null"><![CDATA[ and gainaccount like #{gainAccount} ]]></if>
            <if test="gainAccount_enum != null">
                <![CDATA[ and gainaccount in ]]>
                <foreach collection="gainAccount_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="transferTime != null"><![CDATA[ and transfertime = #{transferTime} ]]></if>
            <if test="transferTime_r_min != null"><![CDATA[ and transfertime >= #{transferTime_r_min} and transfertime < #{transferTime_r_max} ]]></if>
            <if test="messengerBean != null"><![CDATA[ and messengerbean = #{messengerBean} ]]></if>
            <if test="messengerBean_r_min != null"><![CDATA[ and messengerbean >= #{messengerBean_r_min} and messengerbean <= #{messengerBean_r_max} ]]></if>
            <if test="messengerBean_enum != null">
                <![CDATA[ and messengerbean in ]]>
                <foreach collection="messengerBean_enum" item="value" open="(" separator="," close=")">
                    <![CDATA[ #{value} ]]>
                </foreach>
            </if>
            <if test="createTime != null"><![CDATA[ and createtime = #{createTime} ]]></if>
            <if test="createTime_r_min != null"><![CDATA[ and createtime >= #{createTime_r_min} and createtime < #{createTime_r_max} ]]></if>
            <if test="_default_mulattr != null">
                <![CDATA[ and ]]>
                <foreach collection="_default_mulattr" item="item" open="(" separator=" or " close=")">
                    <if test="item.name == 'transferCode'"><![CDATA[ transfercode like #{item.value} ]]></if>
                    <if test="item.name == 'transferAccount'"><![CDATA[ transferaccount like #{item.value} ]]></if>
                    <if test="item.name == 'gainAccount'"><![CDATA[ gainaccount like #{item.value} ]]></if>
                </foreach>
            </if>
        </trim>
    </sql>

    <sql id="orderSql">
        <trim prefix="ORDER BY" suffixOverrides=",">
            <if test="_orderBy == 'transferCode_desc'"><![CDATA[ transfercode desc, ]]></if>
            <if test="_orderBy == 'transferCode_asc'"><![CDATA[ transfercode, ]]></if>
            <if test="_orderBy == 'transferAccount_desc'"><![CDATA[ transferaccount desc, ]]></if>
            <if test="_orderBy == 'transferAccount_asc'"><![CDATA[ transferaccount, ]]></if>
            <if test="_orderBy == 'gainAccount_desc'"><![CDATA[ gainaccount desc, ]]></if>
            <if test="_orderBy == 'gainAccount_asc'"><![CDATA[ gainaccount, ]]></if>
            <if test="_orderBy == 'transferTime_desc'"><![CDATA[ transfertime desc, ]]></if>
            <if test="_orderBy == 'transferTime_asc'"><![CDATA[ transfertime, ]]></if>
            <if test="_orderBy == 'messengerBean_desc'"><![CDATA[ messengerbean desc, ]]></if>
            <if test="_orderBy == 'messengerBean_asc'"><![CDATA[ messengerbean, ]]></if>
            <if test="_orderBy == 'createTime_desc'"><![CDATA[ createtime desc, ]]></if>
            <if test="_orderBy == 'createTime_asc'"><![CDATA[ createtime, ]]></if>
        </trim>
    </sql>

    <insert id="insertObject" parameterType="com.dyb.platforms.fixfunds.services.business.transfer.entity.Transfer">
        insert into
        dyb_transfer(transfercode,transferaccount,gainaccount,transfertime,messengerbean,createtime)
        values(#{transferCode},#{transferAccount},#{gainAccount},#{transferTime},#{messengerBean},#{createTime});
    </insert>

    <update id="updateObject" parameterType="com.dyb.platforms.fixfunds.services.business.transfer.entity.Transfer">
        update dyb_transfer
        set transferaccount=#{transferAccount},gainaccount=#{gainAccount},transfertime=#{transferTime},messengerbean=#{messengerBean},createtime=#{createTime}
        where transfercode=#{transferCode};
    </update>

    <delete id="deleteObject" parameterType="string">
        delete from dyb_transfer where transfercode=#{transferCode};
    </delete>

    <delete id="deleteObjectByWhere" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams">
        delete from dyb_transfer
        <include refid="whereSql" />
    </delete>

    <select id="getBaseObject" parameterType="string" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_transfer where transfercode=#{transferCode};
    </select>

    <select id="getDetailObject" parameterType="string" resultMap="resultMap">
        select * from dyb_transfer where transfercode=#{transferCode};
    </select>

    <select id="queryCount" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultType="int">
        select count(*) from dyb_transfer
          <include refid="whereSql" />
    </select>

    <select id="queryBaseList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select 
        <include refid="baseCol" />
        from dyb_transfer
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>

    <select id="queryDetailList" parameterType="com.dyb.platforms.fixfunds.services.utils.core.QueryParams" resultMap="resultMap">
        select * from dyb_transfer
          <include refid="whereSql" />
        <include refid="orderSql" />
    </select>
</mapper> 


